dependency_overrides:
# where are we? dart/interfaces/<here>
  # types_for_perception:
  #   path: ../../../flutter/utils/perception/types_for_perception
  # json_utils:
  #   path: ../../utils/json_utils